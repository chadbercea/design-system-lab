import { createTheme } from '@mui/material/styles';

/**
 * Base MUI Theme - Stock Configuration
 *
 * This is the starting point for theme experimentation.
 * Goal: Control all design decisions through theme tokens only.
 *
 * Customize this file to explore MUI's theme capabilities.
 */
export const muiTheme = createTheme({
  // Stock MUI theme - no customizations yet
  // This is your playground!
});
